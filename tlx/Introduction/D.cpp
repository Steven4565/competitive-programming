#include <cstdlib>
#include <iostream>
#include <string>
using namespace std;

void correct() { cout << "Tentu saja bisa!" << endl; }

void wrong() { cout << "Wah, tidak bisa :(" << endl; }

int main() {
  string a, b;
  cin >> a >> b;

  int i = 0, j = 0;
  int diffIdx;
  // cout << diffIdxEnd << endl << diffIdxStart << endl;
  // if (abs(diffIdxEnd - diffIdxStart == 2)) {
  //   correct();
  // } else {
  //   wrong();
  // }

  return 0;
}